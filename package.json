{
  "pnpm": {
    "overrides": {
      "node-forge": "1.4.0",
      "@babel/runtime": "7.26.10"
    }
  },
  "name": "sakaba-front",
  "version": "1.0.3",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --filter=web",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@11.0.0"
}
